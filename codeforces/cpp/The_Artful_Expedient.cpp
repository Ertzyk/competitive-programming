#include <iostream>
using namespace std;
int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, x;
    cin >> n;
    for(int i = 0; i < 2*n; i++) cin >> x;
    cout << "Karen\n";
    return 0;
}